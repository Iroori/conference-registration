### 2026-05-19 i18n and Language Fixes

- **Branch:** `main`
- **Session Requirements:** Remove all Korean text from the user-facing UI, specifically addressing the date picker displaying "연 월 일" and any backend error messages leaking into the UI.
- **Results:** Completed.
- **Changed Files:**
  - `[MODIFIED]` `front/index.html`
  - `[MODIFIED]` `back/src/main/java/com/roo/payment/common/exception/ErrorCode.java`
  - `[MODIFIED]` `back/src/main/java/com/roo/payment/domain/iasbse/controller/IasbseMemberController.java`
- **Architectural Decisions (ADR):**
  - **HTML Locale Setting:** Updated `<html lang="ko">` to `<html lang="en">` in `front/index.html`. This ensures native HTML elements like `<input type="date">` render their placeholders and controls in English (e.g., mm/dd/yyyy) rather than Korean (연/월/일) across all browsers.
  - **Backend Error Codes:** Translated all Korean error messages in `ErrorCode.java` to English. Since these messages are propagated directly to the frontend via the `ApiResponse` body, this ensures users will only see English error messages in Toast notifications or inline alerts.
  - **Admin API Responses:** Translated success and failure responses in `IasbseMemberController` to English for consistency across the entire API surface.
